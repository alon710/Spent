/**
 * Canonical GitHub repository for Spent. Single source of truth for every
 * in-app link (sidebar, setup wizard, "open an issue", etc.). Matches the
 * git remote (origin = alon710/Spent).
 */
export const GITHUB_REPO_URL = "https://github.com/alon710/Spent";
export const GITHUB_ISSUES_URL = `${GITHUB_REPO_URL}/issues/new`;
